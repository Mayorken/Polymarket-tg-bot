/** Order side. Mirrors @polymarket/clob-client-v2's Side enum so the Phase 2
 *  adapter can map straight onto it without changing the interface. */
export enum Side {
  BUY = "BUY",
  SELL = "SELL",
}
